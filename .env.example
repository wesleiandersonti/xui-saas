# =============================================================================
# XUI-SaaS Environment Configuration
# =============================================================================
# This is a quick reference for Docker Compose / Root configuration
# For comprehensive configuration options, see .env.example in the project root
# =============================================================================

# =============================================================================
# DATABASE ROOT CONFIGURATION (Docker Compose)
# =============================================================================
# Used only for initial database setup in Docker
DB_ROOT_PASSWORD=your_secure_root_password_min_16_chars

# =============================================================================
# APPLICATION DATABASE USER
# =============================================================================
DB_USER=xui_saas
DB_PASSWORD=your_secure_app_password_min_16_chars
DB_NAME=xui_saas

# =============================================================================
# JWT SECURITY - CRITICAL
# Generate with: openssl rand -base64 64
# =============================================================================
JWT_SECRET=your_super_secret_jwt_key_min_32_chars_replace_this
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# =============================================================================
# ENCRYPTION KEYS
# Must be exactly 32 characters for XUI password encryption
# =============================================================================
XUI_ENCRYPTION_KEY=your-32-char-encryption-key!!

# =============================================================================
# API CONFIGURATION
# =============================================================================
API_URL=http://localhost:5000
# Use specific origins in production, not *
CORS_ORIGIN=http://localhost:3000,http://localhost:5000

# =============================================================================
# EXTERNAL SERVICE INTEGRATIONS (Optional)
# Uncomment and configure as needed
# =============================================================================

# TMDB API for VOD metadata
# TMDB_API_KEY=your_tmdb_api_key_here

# Mercado Pago Payment Gateway
# MERCADOPAGO_ACCESS_TOKEN=your_mercadopago_access_token

# Evolution API for WhatsApp
# EVOLUTION_API_URL=https://your-evolution-api.com
# EVOLUTION_API_KEY=your_evolution_api_key

# Telegram Bot
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
# 1. NEVER commit the actual .env file with real values
# 2. Use different secrets for each environment (dev/staging/prod)
# 3. All secrets should be minimum 32 characters, cryptographically random
# 4. Database passwords should be minimum 16 characters
# 5. Regularly rotate all secrets (every 90 days recommended)
# 6. Use a secrets manager (Vault, AWS Secrets Manager, etc.) in production
# 7. Enable 2FA for all admin accounts
# 8. Use HTTPS/TLS in production
# 9. See full security guide at: ./SECURITY.md
# =============================================================================
